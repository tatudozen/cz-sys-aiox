# DEPRECATED — superseded by deploy-kvm4.yml (Story 1.3)
# This file is kept for git history. Do not use.
name: Deploy (Deprecated)
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use deploy-kvm4.yml instead."
