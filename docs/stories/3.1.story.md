---
story_id: "3.1"
epic: 3
title: "LLM Abstraction Layer & Claude API Client"
status: Approved
priority: Critical
story_type: Architecture
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "typescript", "eslint"]
created_at: "2026-03-14"
created_by: "River (SM Agent)"
approved_by: "Pax (PO Agent)"
approved_at: "2026-03-14"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 3.1: LLM Abstraction Layer & Claude API Client

## Story

**As a** developer (Fernando),
**I want** an abstracted LLM interface with Claude API as primary provider,
**so that** agents can call LLMs consistently and we can add fallback providers later (NFR-10).

---

## Status

Approved

---

## Context

**Epic 3 — Núcleo de Agentes (CMO, Designer, Copywriter)**

Dependência de Epic 1 (monorepo, `packages/agents/` scaffolded). Fundação do agent server — todos os agentes do Epic 3 dependem desta abstração. Implementa o `LLMProvider` interface e `ClaudeProvider` com retry logic.

---

## Acceptance Criteria

- [ ] **AC-1:** `packages/agents/src/llm/` com:
  - Interface `LLMProvider`: `complete(prompt: string, options?: LLMOptions): Promise<string>`
  - Interface `LLMOptions`: `model?`, `maxTokens?`, `temperature?`, `systemPrompt?`
  - Interface `LLMResponse`: `content`, `inputTokens`, `outputTokens`, `model`, `latencyMs`

- [ ] **AC-2:** Implementação `ClaudeProvider` usando `@anthropic-ai/sdk`:
  - Retry logic: 3 tentativas com backoff exponencial (1s, 2s, 4s)
  - Timeout por chamada: 60s (NFR-03)
  - Erro graceful se todas as tentativas falharem

- [ ] **AC-3:** Factory `createLLMProvider(config: LLMConfig): LLMProvider` extensível para futuros providers

- [ ] **AC-4:** Configuração via env vars:
  - `LLM_PROVIDER=claude`
  - `CLAUDE_API_KEY`
  - `CLAUDE_MODEL=claude-sonnet-4-6`

- [ ] **AC-5:** Rate limiting: respeitar limites da Claude API (implementar queue se necessário)

- [ ] **AC-6:** Logging de cada chamada: salvar em tabela `llm_usage_log` no Supabase:
  - `id`, `provider`, `model`, `input_tokens`, `output_tokens`, `total_cost` (USD estimado), `timestamp`

- [ ] **AC-7:** Testes unitários em `packages/agents/src/llm/__tests__/`:
  - Mock do provider → teste de retry (3 falhas → erro)
  - Teste de timeout
  - Teste de factory
  - Fallback graceful quando provider indisponível

- [ ] **AC-8:** Teste de integração: chamada real à Claude API retorna resposta válida

---

## Tasks / Subtasks

- [ ] **Task 1 — Definir interfaces LLMProvider e LLMOptions** (AC: 1)
  - `packages/agents/src/llm/types.ts`
  - [Source: PRD#3.1-AC1, architecture/copyzen-architecture.md#3]

- [ ] **Task 2 — Implementar ClaudeProvider** (AC: 2)
  - `packages/agents/src/llm/providers/claude-provider.ts`
  - Instalar `@anthropic-ai/sdk`
  - Retry com backoff exponencial
  - Timeout 60s
  - [Source: PRD#3.1-AC2, NFR-03]

- [ ] **Task 3 — Factory createLLMProvider** (AC: 3)
  - `packages/agents/src/llm/factory.ts`
  - [Source: PRD#3.1-AC3, NFR-10]

- [ ] **Task 4 — Logging em llm_usage_log** (AC: 6)
  - Verificar tabela na migration existente
  - Criar Supabase client helper em `packages/agents/src/lib/supabase.ts`
  - Após cada chamada: INSERT em llm_usage_log
  - [Source: PRD#3.1-AC6, architecture/copyzen-architecture.md#4.2]

- [ ] **Task 5 — Testes unitários** (AC: 7)
  - Criar `packages/agents/src/llm/__tests__/claude-provider.test.ts`
  - Mock `@anthropic-ai/sdk`
  - [Source: PRD#3.1-AC7, PRD#4.5]

- [ ] **Task 6 — Teste de integração** (AC: 8)
  - `tests/integration/llm.test.ts`
  - Requer `CLAUDE_API_KEY` real (skip se não disponível)
  - [Source: PRD#3.1-AC8]

- [ ] **Task 7 — Instalar dependências** (AC: 2)
  - `@anthropic-ai/sdk` em `packages/agents/`
  - `@supabase/supabase-js` em `packages/agents/`

---

## Dev Notes

### Claude API — Modelo Correto
[Source: architecture/copyzen-architecture.md#3]
Usar `claude-sonnet-4-6` (não claude-3-sonnet, não claude-opus). Confirmar ID exato na documentação Anthropic antes de hardcodar. Configurável via env var `CLAUDE_MODEL`.

### LLMProvider Interface Pattern
[Source: architecture/copyzen-architecture.md#2.5 — Provider Abstraction]
```typescript
interface LLMProvider {
  complete(prompt: string, options?: LLMOptions): Promise<LLMResponse>;
  stream?(prompt: string, options?: LLMOptions): AsyncIterable<string>;
}
```
O método `stream` é opcional no MVP (implementar `complete` apenas). Streams podem ser adicionados na Story 3.3+.

### Custo Estimado Claude API
[Source: PRD#NFR-08]
Modelo sonnet: ~$3/1M input tokens, ~$15/1M output tokens (aproximado). O `total_cost` em `llm_usage_log` é estimativa para tracking de budget ($20-50/mês limite).

### LLMUsageLog Entity
[Source: architecture/copyzen-architecture.md#4.2]
```typescript
interface LLMUsageLog {
  id: string;
  provider: string;
  model: string;
  input_tokens: number;
  output_tokens: number;
  total_cost: number; // USD estimado
  timestamp: string;
}
```

### File Locations
```
packages/agents/src/llm/types.ts
packages/agents/src/llm/providers/claude-provider.ts
packages/agents/src/llm/factory.ts
packages/agents/src/llm/index.ts
packages/agents/src/llm/__tests__/claude-provider.test.ts
packages/agents/src/lib/supabase.ts
tests/integration/llm.test.ts
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Story Progress

### Implementation Checklist
- [ ] Task 1: Interfaces LLM
- [ ] Task 2: ClaudeProvider com retry
- [ ] Task 3: Factory
- [ ] Task 4: Logging llm_usage_log
- [ ] Task 5: Testes unitários
- [ ] Task 6: Teste de integração
- [ ] Task 7: Instalar dependências

### Quality Gates
- [ ] `npm test` — testes unitários passam com mock
- [ ] `npm run typecheck` — TypeScript válido
- [ ] Retry funciona (3 tentativas → erro graceful)
- [ ] Integração real com Claude API retorna resposta válida

---

## File List

### Files to Create
- `packages/agents/src/llm/types.ts`
- `packages/agents/src/llm/providers/claude-provider.ts`
- `packages/agents/src/llm/factory.ts`
- `packages/agents/src/llm/index.ts`
- `packages/agents/src/llm/__tests__/claude-provider.test.ts`
- `packages/agents/src/lib/supabase.ts`
- `tests/integration/llm.test.ts`

---

## Dev Agent Record

### Completion Notes
_(preenchido pelo @dev ao final)_
