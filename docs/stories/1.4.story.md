---
story_id: "1.4"
epic: 1
title: "Docker Swarm Stack para CopyZen Site"
status: Ready for Review
priority: High
story_type: Deployment
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["docker", "traefik", "nginx"]
created_at: "2026-03-14"
created_by: "River (SM Agent)"
approved_by: "Pax (PO Agent)"
approved_at: "2026-03-14"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.4: Docker Swarm Stack para CopyZen Site

## Story

**As a** operator (Fernando),
**I want** a nginx stack compose file ready for Portainer deployment serving `fw.copyzen.com.br`,
**so that** the CopyZen FunWheel is served via nginx with Traefik SSL on the existing Swarm.

---

## Status

Ready for Review

---

## Context

**Epic 1 — Foundation & Infrastructure Setup**

Dependência de 1.1 (monorepo), 1.3 (CI/CD para rsync). Esta story é puramente de infraestrutura — cria os arquivos Docker Compose para serem deployed manualmente via Portainer UI na KVM4.

**Infraestrutura existente na KVM4:**
- Docker Swarm rodando
- Traefik como reverse proxy com certresolver `letsencryptresolver`
- Rede `AZ_Net` (overlay Docker Swarm)
- nginx:alpine disponível
- IP da KVM4: `31.97.26.21`
- Portainer para gestão de stacks

**Nota:** Esta story NÃO faz deploy automaticamente — cria os artefatos para deploy manual via Portainer.

---

## Acceptance Criteria

- [ ] **AC-1:** Arquivo `infra/stacks/copyzen-funwheel.yml` com stack Docker Swarm:
  - Service `nginx:alpine` com volume bind: `/srv/sites/fw.copyzen.com.br:/usr/share/nginx/html:ro`
  - Traefik labels:
    - `traefik.enable=true`
    - `traefik.http.routers.copyzen-fw.rule=Host(\`fw.copyzen.com.br\`)`
    - `traefik.http.routers.copyzen-fw.entrypoints=websecure`
    - `traefik.http.routers.copyzen-fw.tls.certresolver=letsencryptresolver`
    - Redirect HTTP→HTTPS configurado
  - Network: `AZ_Net` (external: true)
  - Resource limits: `cpus: '0.1'`, `memory: 128M`
  - Restart policy: `on-failure`

- [ ] **AC-2:** Arquivo `infra/stacks/copyzen-sales.yml` análogo para futura página de vendas:
  - Mesma estrutura mas com domínio placeholder `sales.copyzen.com.br`
  - Marcado como "preparado para Story 6.3 — não deployar ainda"

- [ ] **AC-3:** Documentação em `docs/infra/deploy-portainer.md` com passo-a-passo:
  - Como fazer upload do stack YAML no Portainer
  - Como criar o stack no Portainer UI
  - Como verificar que o serviço está rodando
  - Como fazer rollback se necessário

- [ ] **AC-4:** Diretório `/srv/sites/fw.copyzen.com.br/` deve ser criado no KVM4 com um `index.html` placeholder (ação manual de Fernando, documentada no guia)

- [ ] **AC-5:** DNS do domínio `fw.copyzen.com.br` apontando para IP `31.97.26.21` (ação manual de Fernando, documentada)

- [ ] **AC-6:** Após deploy via Portainer: `https://fw.copyzen.com.br` retorna o placeholder com SSL válido (Let's Encrypt). Validação manual por Fernando.

---

## Tasks / Subtasks

- [x] **Task 1 — Criar infra/stacks/copyzen-funwheel.yml** (AC: 1)
  - Stack compose com nginx:alpine + Traefik labels + AZ_Net + resource limits
  - Validar sintaxe do compose file
  - [Source: PRD#FR-22, architecture/copyzen-architecture.md#2.2]

- [x] **Task 2 — Criar infra/stacks/copyzen-sales.yml** (AC: 2)
  - Análogo ao funwheel com placeholder domain
  - Adicionar comentário "Para Story 6.3"
  - [Source: PRD#1.4-AC2]

- [x] **Task 3 — Documentar deploy via Portainer** (AC: 3, 4, 5)
  - Criar `docs/infra/deploy-portainer.md`
  - Incluir: criação do diretório no KVM4, configuração DNS, steps do Portainer
  - [Source: PRD#1.4-AC3, NFR-15]

---

## Dev Notes

### Infraestrutura KVM4
[Source: architecture/copyzen-architecture.md#2.2]
- KVM4: 4 CPU, 16 GB RAM, IP 31.97.26.21, Debian 11
- Recursos alocados ao CopyZen nginx: 0.1 CPU, 128 MB RAM
- Rede Docker Swarm: AZ_Net (overlay, external ao stack)
- Traefik já configurado com certresolver `letsencryptresolver`

### CRÍTICO: Portainer UI Only
[Source: PRD#NFR-15]
Deploy de stacks Docker na KVM4 é EXCLUSIVAMENTE via Portainer UI. O arquivo `.yml` é o artefato — Fernando faz o deploy manualmente copiando/colando no Portainer.

### Compose File Pattern
```yaml
version: '3.8'
services:
  copyzen-fw:
    image: nginx:alpine
    volumes:
      - /srv/sites/fw.copyzen.com.br:/usr/share/nginx/html:ro
    networks:
      - AZ_Net
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 128M
      restart_policy:
        condition: on-failure
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.copyzen-fw.rule=Host(`fw.copyzen.com.br`)"
      - "traefik.http.routers.copyzen-fw.entrypoints=websecure"
      - "traefik.http.routers.copyzen-fw.tls.certresolver=letsencryptresolver"
networks:
  AZ_Net:
    external: true
```

### File Locations
```
infra/stacks/copyzen-funwheel.yml  → criar
infra/stacks/copyzen-sales.yml     → criar
docs/infra/deploy-portainer.md     → criar
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Story Progress

### Implementation Checklist
- [x] Task 1: copyzen-funwheel.yml
- [x] Task 2: copyzen-sales.yml
- [x] Task 3: docs/infra/deploy-portainer.md

### Quality Gates
- [x] YAML válido (sem erros de sintaxe docker compose)
- [x] Labels Traefik corretos
- [x] Documentação completa para deploy manual

---

## File List

### Files Created
- `infra/stacks/copyzen-funwheel.yml`
- `infra/stacks/copyzen-sales.yml`
- `docs/infra/deploy-portainer.md`

---

## Dev Agent Record

### Completion Notes
- `infra/stacks/copyzen-funwheel.yml` criado com nginx:alpine + labels Traefik completos (HTTP→HTTPS redirect incluído) + AZ_Net external + resource limits
- `infra/stacks/copyzen-sales.yml` criado análogo — marcado como "NÃO DEPLOYAR — Story 6.3"
- `docs/infra/deploy-portainer.md` com pré-requisitos, steps detalhados do Portainer, verificação SSL, rollback via UI e CLI
- AC-4 (diretório /srv/sites/) e AC-5 (DNS) e AC-6 (validação SSL) requerem ação manual de Fernando — documentados no guia

### Agent Model Used
claude-sonnet-4-6
